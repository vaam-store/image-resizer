# Generated benchmark artifacts, not source.
results/*.json
results/*.log
.venv/

# Deterministically regenerated by fixtures/generate.py (fixed seed, byte-identical
# output) and created on demand by driver/run.sh. ~5MB of binaries that would
# otherwise sit in git for no reason.
fixtures/corpus/
